// Shared contracts and adapters for Smart Itinerary services.
// Populated in T0.3: domain types, zod DTOs, event schemas, adapters
// (db, storage, mailer, broker, jwt, http).
export const SHARED_PACKAGE = "@smart/shared";
